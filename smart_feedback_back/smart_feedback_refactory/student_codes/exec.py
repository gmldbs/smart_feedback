import time, sys, base64
sys.stdout=open('output.out', 'w', encoding='utf8')
def search(x, seq):
    for i, elem in enumerate(seq):
        if x <= elem:
            return i

    return len(seq)

search(-100, ())
, 3, 5, 7, 10))

)
